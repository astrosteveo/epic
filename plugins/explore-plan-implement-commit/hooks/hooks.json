{
  "description": "Inject workflow context at session start",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/init-workflow.sh"
          }
        ]
      }
    ]
  }
}
